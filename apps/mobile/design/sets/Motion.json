{
  "duration": {
    "instant": {
      "$value": "50",
      "$type": "number",
      "$description": "50ms — micro-interactions, ripple start"
    },
    "fast": {
      "$value": "100",
      "$type": "number",
      "$description": "100ms — button feedback, toggle"
    },
    "normal": {
      "$value": "200",
      "$type": "number",
      "$description": "200ms — default transitions"
    },
    "moderate": {
      "$value": "300",
      "$type": "number",
      "$description": "300ms — panel slide, expand"
    },
    "slow": {
      "$value": "500",
      "$type": "number",
      "$description": "500ms — page transition, modal enter"
    },
    "slower": {
      "$value": "700",
      "$type": "number",
      "$description": "700ms — complex choreography"
    }
  },
  "easing": {
    "default": {
      "$value": "cubic-bezier(0.25, 0.1, 0.25, 1)",
      "$type": "other",
      "$description": "Standard ease — most transitions"
    },
    "in": {
      "$value": "cubic-bezier(0.42, 0, 1, 1)",
      "$type": "other",
      "$description": "Ease-in — elements leaving (exit)"
    },
    "out": {
      "$value": "cubic-bezier(0, 0, 0.58, 1)",
      "$type": "other",
      "$description": "Ease-out — elements entering (enter)"
    },
    "in-out": {
      "$value": "cubic-bezier(0.42, 0, 0.58, 1)",
      "$type": "other",
      "$description": "Ease-in-out — moving between positions"
    },
    "spring": {
      "$value": "cubic-bezier(0.34, 1.56, 0.64, 1)",
      "$type": "other",
      "$description": "Spring overshoot — playful bouncy feel"
    },
    "snap": {
      "$value": "cubic-bezier(0.2, 0, 0, 1)",
      "$type": "other",
      "$description": "Quick snap — snackbar appear, toggle"
    }
  }
}
